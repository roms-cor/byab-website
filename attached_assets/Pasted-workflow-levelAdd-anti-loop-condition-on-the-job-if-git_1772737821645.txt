workflow levelAdd anti-loop condition on the job: if: github.actor != 'github-actions[bot]'Files: .github/workflows/release.ymlAcceptance: Workflow file is valid, won't loop, won't conflict with deployT002: Update deploy workflow to avoid double-deployBlocked By: []Details:The release action commits a version bump back to main, which would trigger deploy againAdd combined condition on the build job:if: |  github.actor != 'github-actions[bot]' &&  !startsWith(github.event.head_commit.message, 'chore(release)')Files: .github/workflows/deploy.ymlAcceptance: Deploy runs once per publish, not twiceT003: Update push script to use conventional commit messagesBlocked By: []Details:Change "Publish v${VERSION} — ${DATE}" to "chore(publish): v${VERSION} — ${DATE}"chore: prefix is recognized by conventional-changelog but does NOT trigger a version bump (only feat: and fix: do)Note: Replit's own checkpoint commits won't follow conventional format, but those never reach GitHub — only the push script's commit does. Future feature commits should use feat: / fix: prefixes to trigger releases.Files: script/push-to-github.shAcceptance: Push script produces conventional commitsT004: Document in replit.mdBlocked By: [T001, T002, T003]Details:Document the auto-release workflow, conventional commit format requirementDocument the version sync gap: after a GitHub release bumps package.json version, Replit's copy stays at the old version until manually synced (git pull or manual edit)Note that __APP_VERSION__ in vite.config.ts already reads dynamically from package.json — no hardcoding — so the footer is always correct for whatever version is in the local package.json at build timeDocument conventional commit prefixes: feat: (minor bump), fix: (patch bump), chore: (no bump, but tracked)Files: replit.mdAcceptance: Pipeline fully documented including version sync caveatLe plan est maintenant complet et bien pensé. Tout est cohérent, aucune lacune majeure. Tu peux donner le feu vert à Replit pour exécuter.
Juste deux micro-précisions avant de lancer :
Sur T003 — la note est juste, mais il serait utile que Replit sache qu'il doit préfixer ses futurs commits de features avec feat: ou fix: dès maintenant, pas seulement quand il pense à le faire. Autrement dit, c'est une convention à adopter systématiquement, pas un one-shot. Tu peux le préciser explicitement dans ton instruction à Replit.
Sur T004 — puisque __APP_VERSION__ est déjà lu dynamiquement depuis package.json via Vite, le seul vrai risque résiduel est que Replit déploie avant d'avoir fait un git pull post-release, ce qui afficherait l'ancienne version en prod. Ça vaut la peine de le documenter clairement : "après une release GitHub, faire un git pull dans Replit avant le prochain deploy pour que le footer soit à jour."
Sinon le plan est prêt. Les 4 tâches sont indépendantes (T001, T002, T003 peuvent tourner en parallèle), et T004 attend juste qu'elles soient terminées. Aucun risque de conflit entre elles.